# ─────────────────────────────────────────────
#  Image Analysis — Backend Requirements
# ─────────────────────────────────────────────

# Core image processing
Pillow>=10.0.0            # Image I/O, pixel-level ops, basic metadata
opencv-python>=4.8.0      # Computer vision — sharpness, blur, noise analysis
scikit-image>=0.22.0      # SSIM, structural similarity, advanced metrics
numpy>=1.26.0             # Numerical arrays (dependency of cv2 & skimage)

# EXIF / metadata extraction
exifread>=3.0.0           # Raw EXIF tag reading from JPEG/TIFF/RAW files
piexif>=1.1.3             # Read & write EXIF data

# API framework (FastAPI — recommended for async image upload handling)
fastapi>=0.111.0
uvicorn[standard]>=0.29.0 # ASGI server
python-multipart>=0.0.9   # Required for FastAPI file upload support

# Utilities
python-dotenv>=1.0.0      # Load .env configuration
